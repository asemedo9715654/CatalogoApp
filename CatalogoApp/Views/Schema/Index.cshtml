@model List<CatalogoApp.Models.Tabela>

@{
    ViewBag.Title = "Database Schema";
}

<h2>Database Schema</h2>

<ul>
    @foreach (var table in Model)
    {
        <li>
            <h3>@table.Nome</h3>
            <p>@table.Descricao</p>
            <ul>
                @foreach (var field in table.Fields)
                {
                    <li>
                        <strong>@field.Nome:</strong> @field.Descricao
                    </li>
                }
            </ul>
        </li>
    }
</ul>
